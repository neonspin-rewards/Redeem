/* ================================================================
   js/router.js — STUB (multi-page version).

   In the SPA version, this file handled tab switching and
   leaderboard sort. In the multi-page version:

   - Tab switching is replaced by real <a href="page.html"> navigation.
   - Leaderboard sort is handled directly in app-leaderboard.js.

   This file is kept as a stub so any legacy imports don't break.
   It exports no-op versions of the original functions.
================================================================ */

'use strict';

/** No-op — tab navigation now uses real page links. */
export function initTabNav() {
  // Nothing to do in multi-page mode.
  // Navigation is handled by <a href="..."> anchor tags.
}
